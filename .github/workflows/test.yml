name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -e ".[test]"
      - run: python -m pytest
      - run: unifi-gim build
      - run: git diff --exit-code -- content-pack
      - run: python scripts/graylog_release_gate.py --validate-only --variant with-input
      - run: python scripts/graylog_release_gate.py --validate-only --variant assets-only

